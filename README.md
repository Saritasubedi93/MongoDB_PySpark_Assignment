# MongoDB_PySpark_Assignment
This project implements a small end‑to‑end pipeline using MongoDB Atlas and PySpark with the MongoDB Spark Connector. It loads a sample YouTube API response into MongoDB, reads it into a Spark DataFrame, and extracts only the 'regionCode' for a specific 'channelId'.
